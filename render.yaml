# render.yaml — Render deployment config for full-scraper
#
# This file lives at the root of the full_scraper git repo.
# Render dashboard → New → Blueprint → select your repo.

services:
  - type: web
    name: full-scraper
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: standard
    healthCheckPath: /health
    disk:
      name: scraper-data
      mountPath: /var/data
      sizeGB: 10
    envVars:
      - key: API_KEY
        generateValue: true
      - key: START_YEAR
        value: "1940"
      - key: END_YEAR
        value: "2005"
      - key: PAGE_PAUSE
        value: "0.5"
      - key: BATCH_SIZE
        value: "20"
      - key: AUTO_RESUME
        value: "true"
      - key: DATA_DIR
        value: "/var/data"
